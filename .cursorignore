# .cursorignore - patterns for Cursor AI to ignore

# === Dependency folders ===
# These folders contain third‑party libraries installed by package managers.
# They’re large and rarely useful for AI suggestions.
node_modules/
bower_components/
vendor/

# === Build artifacts ===
# Compiled/transpiled output ends up here; excluding them avoids duplicate context.
dist/
build/
out/
target/

# === Python caches ===
# Python creates cache folders and byte‑code files; ignore them to reduce noise.
__pycache__/
*.py[cod]
*.pyo

# === Logs ===
# Log files can grow quickly and don’t help with code generation.
*.log
logs/

# === Secrets and environment ===
# Store sensitive data (API keys, passwords, etc.) elsewhere and keep it out of AI context.
.env
.env.*
*.pem
*.key
secrets.json
credentials.*

# === Editor and system files ===
# Temporary files created by editors or the OS aren’t relevant to code context.
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db